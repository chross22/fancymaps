% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map-pair.R
\name{map_pair}
\alias{map_pair}
\title{A surface and its uncertainty, as one figure}
\usage{
map_pair(
  x,
  value,
  uncertainty,
  uncertainty_from = NULL,
  by = NULL,
  coords = NULL,
  crs = NULL,
  kind = c("surface", "probability"),
  uncertainty_kind = c("surface", "diverging"),
  uncertainty_midpoint = 0,
  uncertainty_direction = 1,
  labels = NULL,
  titles = NULL,
  ncol = 2,
  coastline = TRUE,
  region = NULL,
  transform = "auto",
  limits = NULL,
  probs = c(0, 0.99),
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  scalebar = TRUE,
  north = TRUE,
  graticule = FALSE,
  base_size = 12
)
}
\arguments{
\item{x}{The geometry, or a \code{map_data}. Both panels are drawn from it unless
\code{uncertainty_from} names a different object.}

\item{value}{What the left panel colours by.}

\item{uncertainty}{What the right panel colours by: a CV, a standard error,
a posterior standard deviation, an extrapolation score.}

\item{uncertainty_from}{A second object to take \code{uncertainty} from, when it
does not live alongside \code{value}. Must cover the same cells.}

\item{by, coords, crs}{Passed to \code{\link[=as_map_data]{as_map_data()}}. \code{crs} is also the CRS the
map is drawn in -- see \code{\link[=display_crs]{display_crs()}} for what happens when it is not
given.}

\item{kind}{How the left panel is scaled: \code{"surface"} for a skewed positive
quantity, \code{"probability"} for one bounded at 0 and 1.}

\item{uncertainty_kind}{How the right panel is scaled. \code{"surface"} for a CV
or an SE, which only increase; \code{"diverging"} for a signed score such as an
extrapolation surface, which needs \code{uncertainty_midpoint}.}

\item{uncertainty_midpoint}{The centre for a diverging right panel. Zero is
the usual meaning and it is the default here, unlike in \code{\link[=map_diverging]{map_diverging()}},
because the caller has already said the panel is diverging.}

\item{uncertainty_direction}{Which way the diverging ramp runs on the right
panel. \code{-1} puts the warm arm at the low end, which is what an
extrapolation surface wants.}

\item{labels}{A length-2 character vector naming the two quantities in their
legends.}

\item{titles}{A length-2 character vector of panel titles.}

\item{ncol}{Panels per row. Two side by side by default; \code{1} stacks them,
which suits a tall study area.}

\item{coastline}{Where land comes from. \code{TRUE} chooses a source for the
extent, \code{FALSE} draws none, a path or an \code{sf} object supplies one. See
\code{\link[=coastline]{coastline()}}.}

\item{region}{An \code{sf} polygon to outline over the map -- the study area, or
whatever the grid was cropped to.}

\item{transform, limits, probs}{Passed to \code{\link[=surface_scale]{surface_scale()}}, which decides how
the values are ramped and says so.}

\item{title, subtitle, caption}{Figure text. The caption is where provenance
belongs: which period, which product, which correction was not applied.
Anything this function had to decide -- a capped scale, a missing
coastline -- is appended to it.}

\item{scalebar, north}{Whether to draw the furniture. See \code{\link[=scale_bar]{scale_bar()}} and
\code{\link[=north_arrow]{north_arrow()}}.}

\item{graticule}{Whether to label coordinates. Off by default; see
\code{\link[=theme_fancymap]{theme_fancymap()}}.}

\item{base_size}{Base font size in points.}
}
\value{
A \pkg{patchwork} object.
}
\description{
A surface and its uncertainty, as one figure
}
\details{
Both panels are drawn on the \strong{same extent} -- the union of the two, so
neither is cropped -- in the \strong{same projection}, with the \strong{same coastline
object}, and the furniture is drawn on the left panel only, since a scale
bar repeated on an identical extent is furniture twice.

The two legends stay separate, and deliberately: they are different
quantities in different units, and a shared one would be a lie. What they
share is position, size and typography, which is what makes the pairing
read.
}
\examples{
grid <- example_grid()

map_pair(grid, "density", "cv",
         labels = c("animals per km2", "CV"))

# the projection-and-extrapolation pair
map_pair(grid, "density", "mess",
         uncertainty_kind = "diverging", uncertainty_direction = -1,
         labels = c("animals per km2", "MESS"),
         titles = c("Predicted density",
                    "How familiar these conditions are"))

}
