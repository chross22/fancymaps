% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map-effort.R
\name{map_effort}
\alias{map_effort}
\title{Map survey effort and detections}
\usage{
map_effort(
  tracks = NULL,
  points = NULL,
  value = NULL,
  size = NULL,
  bin = NA,
  bins = 40,
  fun = NULL,
  coords = NULL,
  crs = NULL,
  label = NULL,
  coastline = TRUE,
  region = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  scalebar = TRUE,
  north = TRUE,
  graticule = FALSE,
  base_size = 12,
  theme = NULL,
  bin_threshold = 2000,
  expand = 0.02
)
}
\arguments{
\item{tracks}{An \code{sf} object of lines -- the tracklines -- or \code{NULL}.}

\item{points}{An \code{sf} object of points: segment midpoints, or sightings.}

\item{value}{A column of \code{points} to colour by, such as group size. \code{NULL}
draws them in one colour.}

\item{size}{A column of \code{points} to size by. Group size is the usual one,
and sizing by it rather than colouring by it leaves colour free for
something else.}

\item{bin}{Whether to aggregate the points into hexagonal bins instead of
drawing them. \code{NA} (the default) decides from how many there are; \code{TRUE}
and \code{FALSE} force it. See below.}

\item{bins}{How many bins across the extent, when binning.}

\item{fun}{How to summarise \code{value} within a bin. \code{"count"} when no \code{value}
is given, which is what an effort map wants.}

\item{coords, crs}{Passed to \code{\link[=as_map_data]{as_map_data()}}. \code{crs} is also the CRS the map
is drawn in -- see \code{\link[=display_crs]{display_crs()}}.}

\item{label}{What to call the value in the legend. Defaults to the column
name.}

\item{coastline}{Where land comes from. \code{TRUE} chooses a source for the
extent, \code{FALSE} draws none, a path or an \code{sf} object supplies one. See
\code{\link[=coastline]{coastline()}}.}

\item{region}{An \code{sf} polygon to outline over the map -- the study area, or
whatever the grid was cropped to.}

\item{title, subtitle, caption}{Figure text. The caption is where provenance
belongs: which period, which product, which correction was not applied.
Anything this function had to decide -- a capped scale, a missing
coastline -- is appended to it.}

\item{scalebar, north}{Whether to draw the furniture. See \code{\link[=scale_bar]{scale_bar()}} and
\code{\link[=north_arrow]{north_arrow()}}.}

\item{graticule}{Whether to label coordinates. Off by default; see
\code{\link[=theme_fancymap]{theme_fancymap()}}.}

\item{base_size}{Base font size in points.}

\item{theme}{A theme to use instead of \code{\link[=theme_fancymap]{theme_fancymap()}}.}

\item{bin_threshold}{How many points before binning becomes the default.
2,000, which is roughly where overlap starts hiding structure at ordinary
figure sizes.}

\item{expand}{How much margin to leave around the data, as a fraction of its
own extent. Widen it when the data does not reach anything a reader can
orient by -- a small grid in open water shows no coastline at all until the
panel is wide enough to include one.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Tracklines, segment midpoints and sightings: where the survey went and what
it saw. The map that says what the model was fitted from, and the one a
reader checks a prediction against.
}
\section{Binning}{
Eight thousand segment midpoints on one map is a black smear, and it is a
smear that looks like data. Past \code{bin_threshold} points the default is to
bin them with \code{\link[fancyfx:hex_bin]{fancyfx::hex_bin()}} and say so, rather than to draw a figure
whose density is set by the plotting order.

The threshold is 2,000, which is roughly where overlap starts to hide
structure at ordinary figure sizes. Force it either way when you know
better: \code{bin = FALSE} on a sparse map, \code{bin = TRUE} on a dense one that
happens to fall under the line.
}

\examples{
pts <- sf::st_as_sf(
  data.frame(lon = runif(300, -70.4, -68.1), lat = runif(300, 42.6, 44.3),
             group = rpois(300, 3)),
  coords = c("lon", "lat"), crs = 4326)

map_effort(points = pts, size = "group", title = "Sightings")

}
