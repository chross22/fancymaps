% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_fancymap_panel}
\alias{theme_fancymap_panel}
\title{The map theme as used inside a multi-panel figure}
\usage{
theme_fancymap_panel(
  base_size = 12,
  base_family = "",
  legend = "bottom",
  graticule = FALSE,
  ...
)
}
\arguments{
\item{base_size}{Base font size in points. Everything else is a multiple of
it, so raising it scales the figure and stays balanced.}

\item{base_family}{Base font family. Empty lets the device choose.}

\item{legend}{Legend position: \code{"right"}, \code{"bottom"}, \code{"top"}, \code{"left"} or
\code{"none"}. \code{"right"} suits a tall panel and \code{"bottom"} a wide one, which is
why the paired and panelled layouts change it.}

\item{graticule}{Whether to draw coordinate labels and gridlines. \code{FALSE} by
default on the reasoning above. Set \code{TRUE} when the coordinates are the
point -- a locator map, or a figure whose caption cites positions.}

\item{...}{Passed to \code{\link[fancyfx:theme_fancyfx]{fancyfx::theme_fancyfx()}}, so its per-element size
arguments all work.}
}
\value{
A \pkg{ggplot2} theme object.
}
\description{
The same theme with the per-panel title made smaller and the margins tightened,
so that two maps sitting side by side read as one figure rather than as two
figures that happen to be adjacent.
}
\examples{
theme_fancymap_panel()

}
