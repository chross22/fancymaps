% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leaflet.R
\name{leaflet_panels}
\alias{leaflet_panels}
\title{A series over several periods, interactively}
\usage{
leaflet_panels(
  x,
  values,
  by = NULL,
  coords = NULL,
  crs = NULL,
  titles = NULL,
  kind = c("surface", "probability", "diverging"),
  midpoint = NULL,
  direction = 1,
  label = NULL,
  transform = "auto",
  limits = NULL,
  probs = c(0, 0.99),
  popup = NULL,
  tiles = "CartoDB.Positron",
  opacity = 0.8,
  legend = TRUE,
  position = "topright"
)
}
\arguments{
\item{x}{The geometry -- one grid, drawn once per set of values.}

\item{values}{The panels. One of:
\itemize{
\item a character vector of column names in \code{x};
\item a matrix with one row per cell and one column per period, which is the
form an averaging or posterior step emits -- column names become panel
titles;
\item a named list of vectors, each in feature order or named by \code{by}.
}}

\item{by, coords, crs}{Passed to \code{\link[=as_map_data]{as_map_data()}}. \code{crs} is also the CRS the
map is drawn in -- see \code{\link[=display_crs]{display_crs()}} for what happens when it is not
given.}

\item{titles}{Panel titles. Taken from the names of \code{values} when not given.}

\item{kind}{How the values are scaled: \code{"surface"}, \code{"probability"} or
\code{"diverging"}.}

\item{midpoint, direction}{For \code{leaflet_diverging()}, as in \code{\link[=map_diverging]{map_diverging()}}.}

\item{label}{What to call the quantity in the single shared legend.}

\item{transform, limits, probs}{Passed to \code{\link[=surface_scale]{surface_scale()}}, so the colours
match the static map's exactly.}

\item{popup}{Extra columns of \code{x} to show when a cell is clicked, as a
character vector. The value and the join key are always shown. \code{FALSE}
turns popups off.}

\item{tiles}{A \pkg{leaflet} provider name for the basemap, or \code{NULL} for no
basemap at all. The default is a quiet grey one, so the data is the
brightest thing on the map rather than competing with a road network.}

\item{opacity}{How opaque the cells are over the basemap.}

\item{legend}{Whether to draw the legend.}

\item{position}{Where the layer control sits.}
}
\value{
A \pkg{leaflet} widget.
}
\description{
The interactive counterpart of \code{\link[=map_panels]{map_panels()}}: every period on one map, one
shared scale, and a control to step through them.
}
\section{One legend, and it does not move}{
The scale is computed once over every period pooled, exactly as in
\code{\link[=map_panels]{map_panels()}} -- both call the same \code{pooled_spec()}, so a cell of a given
value is the same colour in the static figure and this one.

Because the scale is shared, the legend is drawn \strong{once and left alone}
rather than tied to each layer. Stepping through the periods changes the map
and not the legend, which is what makes the comparison readable: a colour
that means 0.4 in spring still means 0.4 in autumn, and the legend sitting
still is the visible evidence of that.

It is also the interactive form that suits a series best. Small multiples ask
a reader to compare across a page; stepping through them in place compares by
change-blindness instead, which is far more sensitive to a small shift and
needs no alignment at all.
}

\examples{
\dontrun{
grid <- example_grid()
seasons <- cbind(spring = grid$density,
                 summer = grid$density * 2.5,
                 autumn = grid$density * 0.4)

leaflet_panels(grid, seasons, label = "animals per km2")
}

}
\seealso{
\code{\link[=map_panels]{map_panels()}}, the static original.
}
