% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map-panels.R
\name{map_panels}
\alias{map_panels}
\title{The same geography over several periods, on one scale}
\usage{
map_panels(
  x,
  values,
  by = NULL,
  coords = NULL,
  crs = NULL,
  titles = NULL,
  kind = c("surface", "probability", "diverging"),
  midpoint = NULL,
  direction = 1,
  ncol = NULL,
  label = NULL,
  coastline = TRUE,
  region = NULL,
  transform = "auto",
  limits = NULL,
  probs = c(0, 0.99),
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  scalebar = TRUE,
  north = TRUE,
  graticule = FALSE,
  base_size = 11
)
}
\arguments{
\item{x}{The geometry -- one grid, drawn once per set of values.}

\item{values}{The panels. One of:
\itemize{
\item a character vector of column names in \code{x};
\item a matrix with one row per cell and one column per period, which is the
form an averaging or posterior step emits -- column names become panel
titles;
\item a named list of vectors, each in feature order or named by \code{by}.
}}

\item{by, coords, crs}{Passed to \code{\link[=as_map_data]{as_map_data()}}. \code{crs} is also the CRS the
map is drawn in -- see \code{\link[=display_crs]{display_crs()}} for what happens when it is not
given.}

\item{titles}{Panel titles. Taken from the names of \code{values} when not given.}

\item{kind}{How the values are scaled: \code{"surface"}, \code{"probability"} or
\code{"diverging"}.}

\item{midpoint}{The centre, for \code{kind = "diverging"}.}

\item{direction}{Which way the ramp runs; \code{-1} reverses it.}

\item{ncol}{Panels per row. Chosen to keep the figure roughly square when
not given.}

\item{label}{What to call the quantity in the single shared legend.}

\item{coastline}{Where land comes from. \code{TRUE} chooses a source for the
extent, \code{FALSE} draws none, a path or an \code{sf} object supplies one. See
\code{\link[=coastline]{coastline()}}.}

\item{region}{An \code{sf} polygon to outline over the map -- the study area, or
whatever the grid was cropped to.}

\item{transform, limits, probs}{Passed to \code{\link[=surface_scale]{surface_scale()}}, which decides how
the values are ramped and says so.}

\item{title, subtitle, caption}{Figure text. The caption is where provenance
belongs: which period, which product, which correction was not applied.
Anything this function had to decide -- a capped scale, a missing
coastline -- is appended to it.}

\item{scalebar, north}{Whether to draw the furniture. See \code{\link[=scale_bar]{scale_bar()}} and
\code{\link[=north_arrow]{north_arrow()}}.}

\item{graticule}{Whether to label coordinates. Off by default; see
\code{\link[=theme_fancymap]{theme_fancymap()}}.}

\item{base_size}{Base font size in points.}
}
\value{
A \pkg{patchwork} object.
}
\description{
Six seasons of occupancy, twelve months of a projected surface, four
candidate models over the same grid. One shared colour scale across every
panel, because panels drawn separately cannot be compared and look as though
they can.
}
\section{The shared scale is the point}{
Limits and transform are computed once over \strong{every} panel's values pooled
together, then applied to all of them. A per-panel scale makes each panel a
picture of its own relative pattern, and lays them out in a grid that
invites reading across -- so a quiet season and a busy one look identical
and the difference between them, the only thing a series is for, is the one
thing that has been scaled away.

This is also why the transform is fixed once. \code{\link[=surface_scale]{surface_scale()}}'s automatic
choice depends on the data it sees; letting each panel choose would give a
log scale to the skewed months and a linear one to the flat ones.
}

\section{Extent and land}{
One extent, one projection, one coastline object, resolved once and used by
every panel. The scale bar and north arrow are drawn on the first panel
only.
}

\examples{
grid <- example_grid()

# three periods held on one scale
seasons <- cbind(spring = grid$density,
                 summer = grid$density * 2.5,
                 autumn = grid$density * 0.4)
map_panels(grid, seasons, label = "animals per km2")

}
