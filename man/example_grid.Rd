% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-data.R
\name{example_grid}
\alias{example_grid}
\title{A small example grid}
\usage{
example_grid(seed = 42)
}
\arguments{
\item{seed}{The random seed. Fixed by default, so the examples draw the same
figure every time and a change to one is a change worth looking at.}
}
\value{
An \code{sf} data frame of polygons in WGS84.
}
\description{
A 12 by 9 grid of square cells in the Gulf of Maine, carrying one column of
each kind of quantity this package draws. Used by the examples and the
tests, so that neither needs a fitted model, a covariate product or the
network.
}
\details{
The values are made up, but not arbitrarily: each is shaped like the
quantity it stands for, because the point of the fixture is to exercise the
scales.

\describe{
\item{\code{density}}{Skewed hard, most cells near zero, a few carrying most of
the total -- which is what makes a linear ramp useless and is the case
\code{\link[=surface_scale]{surface_scale()}} exists for.}
\item{\code{cv}}{Rising where density falls, as a coefficient of variation
does: the model is least certain where it saw least.}
\item{\code{mess}}{Mostly positive with a novel corner, so it diverges around a
zero that means something.}
\item{\code{occupancy}}{On [0, 1], for \code{\link[=map_probability]{map_probability()}}.}
\item{\code{residual}}{Centred on a non-zero mean, which is the case that
breaks a diverging scale centred on zero.}
\item{\code{grid_id}}{A cell identifier, for exercising the join in
\code{\link[=as_map_data]{as_map_data()}}.}
}
}
\examples{
grid <- example_grid()
head(sf::st_drop_geometry(grid))

}
