% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map-data.R
\name{as_map_data}
\alias{as_map_data}
\title{Put spatial model output into the form the maps draw from}
\usage{
as_map_data(
  x,
  value = NULL,
  by = NULL,
  coords = NULL,
  crs = NULL,
  label = NULL
)
}
\arguments{
\item{x}{The geometry: an \code{sf} object of polygons, points or lines, a
\pkg{terra} \code{SpatRaster}, or a data frame with coordinate columns.}

\item{value}{What to colour by. One of:
\itemize{
\item a string naming a column of \code{x};
\item an unnamed numeric vector, one element per feature of \code{x}, in the same
order;
\item a \strong{named} numeric vector, matched against \code{x[[by]]} by name -- the
form a posterior summary or a lookup table arrives in;
\item \code{NULL}, for geometry drawn without a colour, such as tracklines.
}}

\item{by}{The column of \code{x} holding the identifier that a named \code{value} is
keyed by. Guessed from \code{grid_id}, \code{id} or \code{cell} when not given.}

\item{coords}{For a data frame, the two coordinate columns, as
\code{c("x", "y")}. Guessed from the usual spellings when not given.}

\item{crs}{The coordinate reference system the coordinates are in. Required
for a data frame, since nothing about two numeric columns says whether
they are degrees or metres. Ignored when \code{x} already carries one.}

\item{label}{What to call the value in a legend. Defaults to the column
name, or to the name of the expression passed as \code{value}.}
}
\value{
An object of class \code{map_data}: a list with \code{geometry} (an \code{sfc}),
\code{value} (numeric or \code{NULL}), \code{kind} (\code{"polygon"}, \code{"point"}, \code{"line"} or
\code{"raster"}), \code{label}, and \code{data}, a data frame of the non-geometry columns.
}
\description{
Called for you by \code{\link[=map_surface]{map_surface()}} and friends. Call it directly when you
want to inspect what they will draw, or to normalise something once and draw
it several times.
}
\section{Checking the join}{
When \code{value} is named, every name must be found in \code{x[[by]]}, and the
function stops if any is not, naming the first few missing keys. It does not
quietly drop them: a partial join produces a map with holes in it that looks
exactly like a region where the model had nothing to say.

Keys are compared as character. An integer \code{grid_id} and a vector named
\code{"1"}, \code{"2"}, \code{"3"} are the common case and they match.
}

\section{Rasters}{
A \code{SpatRaster} is read into cell centres and drawn through a raster path.
It is deliberately not polygonised: \code{terra::as.polygons()} on a grid of any
size is slow enough to be the dominant cost of drawing the figure, and the
result is a polygon per cell that is then drawn as a rectangle anyway.
}

\examples{
grid <- sf::st_sf(
  grid_id = 1:2,
  geometry = sf::st_sfc(
    sf::st_polygon(list(cbind(c(0, 1, 1, 0, 0), c(0, 0, 1, 1, 0)))),
    sf::st_polygon(list(cbind(c(1, 2, 2, 1, 1), c(0, 0, 1, 1, 0)))),
    crs = 4326))

# a column that is already there
grid$depth <- c(40, 120)
as_map_data(grid, "depth")

# values that arrived separately, keyed by id
occupancy <- c("2" = 0.8, "1" = 0.3)
as_map_data(grid, occupancy, by = "grid_id")

}
