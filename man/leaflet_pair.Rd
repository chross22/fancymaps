% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leaflet.R
\name{leaflet_pair}
\alias{leaflet_pair}
\title{A surface and its uncertainty, interactively}
\usage{
leaflet_pair(
  x,
  value,
  uncertainty,
  uncertainty_from = NULL,
  by = NULL,
  coords = NULL,
  crs = NULL,
  kind = c("surface", "probability"),
  uncertainty_kind = c("surface", "diverging"),
  uncertainty_midpoint = 0,
  uncertainty_direction = 1,
  labels = NULL,
  transform = "auto",
  limits = NULL,
  probs = c(0, 0.99),
  popup = NULL,
  tiles = "Esri.WorldGrayCanvas",
  opacity = 0.8,
  position = "topright"
)
}
\arguments{
\item{x}{The geometry, or a \code{map_data}. Both panels are drawn from it unless
\code{uncertainty_from} names a different object.}

\item{value}{What the left panel colours by.}

\item{uncertainty}{What the right panel colours by: a CV, a standard error,
a posterior standard deviation, an extrapolation score.}

\item{uncertainty_from}{A second object to take \code{uncertainty} from, when it
does not live alongside \code{value}. Must cover the same cells.}

\item{by, coords, crs}{Passed to \code{\link[=as_map_data]{as_map_data()}}. \code{crs} is also the CRS the
map is drawn in -- see \code{\link[=display_crs]{display_crs()}} for what happens when it is not
given.}

\item{kind}{How the left panel is scaled: \code{"surface"} for a skewed positive
quantity, \code{"probability"} for one bounded at 0 and 1.}

\item{uncertainty_kind}{How the right panel is scaled. \code{"surface"} for a CV
or an SE, which only increase; \code{"diverging"} for a signed score such as an
extrapolation surface, which needs \code{uncertainty_midpoint}.}

\item{uncertainty_midpoint}{The centre for a diverging right panel. Zero is
the usual meaning and it is the default here, unlike in \code{\link[=map_diverging]{map_diverging()}},
because the caller has already said the panel is diverging.}

\item{uncertainty_direction}{Which way the diverging ramp runs on the right
panel. \code{-1} puts the warm arm at the low end, which is what an
extrapolation surface wants.}

\item{labels}{A length-2 character vector naming the two quantities. These
are what the switch is labelled with, so they should read as names rather
than as units.}

\item{transform, limits, probs}{Passed to \code{\link[=surface_scale]{surface_scale()}}, so the colours
match the static map's exactly.}

\item{popup}{Extra columns of \code{x} to show when a cell is clicked, as a
character vector. The value and the join key are always shown. \code{FALSE}
turns popups off.}

\item{tiles}{A \pkg{leaflet} provider name for the basemap, or \code{NULL} for no
basemap at all. The default is \code{"Esri.WorldGrayCanvas"}, a quiet grey one,
so the data is the brightest thing on the map rather than competing with a
road network. It is also key-free: see the note on \code{leaflet_base()}.}

\item{opacity}{How opaque the cells are over the basemap.}

\item{position}{Where the layer control sits.}
}
\value{
A \pkg{leaflet} widget.
}
\description{
The interactive counterpart of \code{\link[=map_pair]{map_pair()}}: both quantities on one map, with
a control to switch between them.
}
\section{Why one map and a switch, rather than two maps side by side}{
\code{\link[=map_pair]{map_pair()}} draws two panels because a static figure has no other way to
show two things at once, and it goes to some trouble -- shared extent, shared
projection, one coastline, aligned panels -- to make them comparable cell by
cell. All of that is machinery for approximating, on paper, something an
interactive map gets for free.

Switching layers on one map is a \strong{blink comparison}: the same cells, at the
same position and the same zoom, changing only in the quantity drawn. Nothing
has to be aligned because nothing moved. Two side-by-side widgets would
reintroduce exactly the alignment problem \code{map_pair()} exists to solve, and
would need a synchronisation dependency to solve it again.

The trade is that the two can no longer be seen simultaneously. When that is
what you want -- a figure for a manuscript, or a reader who cannot click --
\code{\link[=map_pair]{map_pair()}} is the one to use.

Each layer carries its own legend, shown and hidden with it, because the two
are different quantities in different units.
}

\examples{
\dontrun{
grid <- example_grid()

leaflet_pair(grid, "density", "cv")

leaflet_pair(grid, "density", "mess", uncertainty_kind = "diverging",
             uncertainty_direction = -1,
             labels = c("density", "extrapolation"))
}

}
\seealso{
\code{\link[=map_pair]{map_pair()}}, the static original.
}
