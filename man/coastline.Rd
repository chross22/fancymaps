% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coastline.R
\name{coastline}
\alias{coastline}
\alias{coastline_fixture}
\title{A coastline for a map}
\usage{
coastline(extent = NULL, source = TRUE, crs = NULL, pad = 0.05)

coastline_fixture()
}
\arguments{
\item{extent}{Anything with a bounding box -- an \code{sf} object, a \code{bbox}, a
\code{map_data} -- naming the area to be covered. \code{NULL} returns the whole
source, uncropped.}

\item{source}{Where land comes from:
\itemize{
\item \code{TRUE} (default) -- choose automatically: the bundled fixture if it
covers the extent, otherwise \pkg{rnaturalearth} at a resolution picked
from the extent;
\item \code{FALSE} -- no land, deliberately. The only way to get a map with no
shoreline and no complaint about it;
\item a path to a shapefile or any other format \code{\link[sf:st_read]{sf::st_read()}} reads. The
better option at bay scale;
\item an \code{sf} object already in memory.
}}

\item{crs}{The CRS to return the land in. Defaults to the extent's.}

\item{pad}{How far past the extent to keep land, as a fraction of the
extent's width. A little overhang stops the shoreline stopping dead at the
panel edge.}
}
\value{
An \code{sf} object of land polygons, or \code{NULL} if \code{source = FALSE} or
nothing could be resolved.
}
\description{
Resolves a land layer for a given extent, choosing a resolution to suit it.
Called for you by every map in this package; call it directly to inspect
what will be drawn, or to fetch once and reuse across many figures.
}
\section{Which resolution}{
Chosen from the width of the extent, because that is what decides whether
the generalisation in a coastline is visible:\tabular{lll}{
   extent width \tab resolution \tab Natural Earth scale \cr
   over 1,500 km \tab \code{"small"} \tab 1:110m \cr
   200 to 1,500 km \tab \code{"medium"} \tab 1:50m \cr
   under 200 km \tab \code{"large"} \tab 1:10m \cr
}


\code{"large"} needs the \code{rnaturalearthhires} package, which is not on CRAN. If
it is not installed, \code{"medium"} is used and a warning says the coastline is
coarser than the map, since that is a defect a reader will otherwise
attribute to the data.
}

\section{The bundled fixture}{
A cropped copy of Natural Earth medium covering the Gulf of Maine and the
Bay of Fundy ships with the package, so that examples draw land and tests
never touch the network. It is used automatically when it covers the
requested extent, and it is the same data \pkg{rnaturalearth} would return
at that scale -- just already here.
}

\examples{
box <- sf::st_bbox(c(xmin = -70.5, ymin = 42.5, xmax = -68, ymax = 44.5),
                   crs = sf::st_crs(4326))
land <- coastline(box)
nrow(land)

# deliberately none
coastline(box, source = FALSE)

}
